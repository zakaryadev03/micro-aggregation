# Docker
# Build a Docker image
# https://docs.microsoft.com/azure/devops/pipelines/languages/docker

trigger:
  branches:
    include:
      - master
  paths:
    include:
      - amazon/*

resources:
- repo: self

variables:
  tag: '$(Build.BuildId)'

stages:
- stage: Build
  displayName: Build image
  jobs:
  - job: Build
    displayName: Build
    pool:
      vmImage: ubuntu-latest
    steps:
    - task: Docker@2
      inputs:
        containerRegistry: 'DockerHub'
        repository: 'zakaryab2003/amazon-prod'
        command: 'buildAndPush'
        Dockerfile: 'amazon/Dockerfile'
        buildContext: 'amazon'
        tags: |
          $(Build.BuildId)
          latest

- stage: Deploy
  jobs:
  - job: Deploy
    pool:
      vmImage: ubuntu-latest
    steps:
    - task: SSH@0
      displayName: SSH into VM and update Docker Compose
      inputs:
        sshEndpoint: 'deploy-vm'
        runOptions: 'inline'
        inline: |
          cd /home/azureuser/micro-aggregation
          docker compose pull
          docker compose up -d